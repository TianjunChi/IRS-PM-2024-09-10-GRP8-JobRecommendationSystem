<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usermanager.mapper.JobMapper">
    <select id="getDefaultJobs" resultType="com.example.usermanager.entity.Job">
        select * from job
            LIMIT 8;
    </select>
</mapper>